[package]
name = "compose_parser"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust parser created to parse the Jetpack Compose syntax tree"
authors = ["Dzhos Oleksii"]

[dependencies]
peg = "0.8.2"
